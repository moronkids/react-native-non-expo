import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
    card: {
        width: '100%',
        borderRadius: 20,
        overflow: 'hidden',
        "boxShadow": "0px 7px 22px -14px rgba(0,0,0,0.75)"
    }
})